# Exclude runtime database and secrets from package (security).
data/database.db
*.db
.env
.env.*

# Packaging is controlled by package.json "files" (dist, dashboard-dist, data/agents, data/skills, data/templates).
